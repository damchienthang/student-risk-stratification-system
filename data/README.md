# Data Directory

This directory contains the dataset used by the Student Risk Stratification System:
- `raw/`: Raw CSV files from the Open University Learning Analytics Dataset (OULAD).
- `processed/`: Preprocessed and labeled student features table (`student_features_labeled.csv`).
